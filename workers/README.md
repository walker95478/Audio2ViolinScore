# Workers

Melody and MuScriptor workers will be added in later phases. Each worker must keep its own virtual environment under `workers/<name>/.venv`; no worker environment or model cache belongs in Git.
