# Benchmark reports

Only lightweight, reproducible benchmark metadata may be committed here:

- JSON metrics;
- Markdown summaries;
- small text metadata.

Do not commit source audio, stems, model weights, PDFs, MIDI, large binaries, or private/user data. Runtime artifacts remain under ignored `output/`, `cache/`, and `temp/` directories.
